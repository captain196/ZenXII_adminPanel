# 05d · Adversary rows — Document Engine (Certificates)

**Agent: A11 · ADVERSARY.** E2 ceiling — every row below is **authored, not executed**. All
rows ship `Status = NOT TESTED`, `Actual Result` blank, no `Evidence Level` (nothing was
observed). Ready for A12 to merge into `06-uat-matrix.csv` in the §15.2 column shape. Test
IDs are prefixed `A11-<method>-<n>` so provenance survives the merge; A12 should renumber
into the sheet's own `T#-##` scheme on merge, keeping the `A11-...` id in `Remarks` for
traceability.

Two rows (`A11-M1-01`, `A11-M1-02`) are the two exploit chains this pass found and wrote up
in full in `05-risk-register.md` (OV1, OV2) — they are the highest-value rows in this file
and should be the first two executed once Stop Gate A opens runtime testing.

Column order, per `zenxii-uat-agent-team-v3.md` §15.2:
`Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected
Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status |
Evidence Level | Device/Build | Remarks`

---

## M1 · Mutation (role / tenant / freshness / duplicate / order / state)

| Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status | Evidence Level | Device/Build | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A11-M1-01 | T0 | Admin panel | Publish / proof durability | Staff (edit) | A template with `publishedVersion=6`, a real frozen `documentTemplateVersions/{id}_v6` snapshot, and a proof PDF on disk at the v6 path. | 1. `save(id, {version:6}, lockVersion)`. 2. Edit the design (any visible change). 3. `proof_pdf(id)`. 4. As any `view`-grade user, `version_pdf(id, version=6, lang)`. | `version:6` in step 1's patch body; any design edit in step 2. | The v6 PDF served in step 4 is **byte-identical to what was originally published as v6** — it must NOT reflect step 2's edit. If it does, the "immutable version" guarantee is defeated. | Implementation-derived — `Doc_template_service.php:335-339` (strip list omits `version`), `Doc_templates.php:826,839,857-860` (filename keyed only to live `version`, no `publishedVersion` check) | `05-risk-register.md` OV1 | I2/N2 (a published version is never rewritten) | **The frozen certificate record for an already-published version is silently rewritable — the single most severe finding this pass** |  | NOT TESTED |  |  | A11-M1 · overwrite-published-proof. Run this FIRST. |
| A11-M1-02 | T0 | Admin panel | Publish / state-gate | Staff (edit) | A `custom:*` template (state-gate-exempt), proofed. School is state-gated OUT of some statutory type (e.g. Madhya Pradesh school, target type `study` = AP-only). | 1. `proof_pdf(id)` on the custom template. 2. `save(id, {docType:"study"}, lockVersion)` — do NOT touch `page/header/footer/objects/languages/defaultLanguage`. 3. `publish(id)`. 4. Read the resulting `documentTemplateVersions` doc. | `docType:"study"` (or any state-gated type this school cannot create via `create()`) | `publish()` must REFUSE — a school not entitled to create a type must not be able to freeze a permanent record under that type. If it succeeds, the snapshot's `docType` field reads the unauthorized value. | Implementation-derived — `Doc_template_service.php:372-382` (contentHash omits `docType`), `:527` (snapshot copies `docType` from the live head unchecked), `Doc_templates.php:1095-1114,606` (`_safe_type()` never called from `save()`/`publish()`) | `05-risk-register.md` OV2 | N4 (proof gate), N9 (strip-list should be exhaustive) | **A state-gate bypass becomes a permanent, undeletable record — escalates L9/I-11 from P2 to P1** |  | NOT TESTED |  |  | A11-M1 · docType-launder-through-publish. Run SECOND. |
| A11-M1-03 | T1 | Admin panel | RBAC surfacing | Staff (edit, NOT manage) | Session holds `Certificates: edit` only. | Open a template. Click Publish. Click "Make live" (activate). Click Deactivate. | n/a | Every one of the three buttons is either hidden/disabled for this role, or the server responds 403 and the UI shows a clear, role-specific denial — not a generic toast the user could mistake for a transient failure. | Implementation-derived — `01a-web-spec.md` §8 (`SRV.can.manage` gates only Delete + Rollback; Publish/Activate/Deactivate render unconditionally) | — | RBAC parity between UI affordance and server grant | An `edit`-only clerk discovers mid-action that they lack permission, via a generic toast, with no advance warning | | NOT TESTED | | | A11-M1 · role mutation (edit→attempted manage action) |
| A11-M1-04 | T1 | Admin panel | Tenant isolation | Staff (any grade), School A | A REAL template id belonging to a second school, School B (seeded fixture — this is the H1/H2 gap every prior pass left open). | As School A's session, call `get_template`, `get_versions`, `version_pdf`, `save`, `publish`, `activate`, `deactivate`, `delete`, `archive` against School B's real id. | School B's real `templateId` | Every call refuses with the SAME message/code as a genuinely-absent id — no existence oracle, no data returned, no write applied. | Implementation-derived — `Doc_template_service.php:888-900` (`head()`); `05a-security.md` §1c (this exact test was named but never executed — a fixture was never available) | `03-invariants.md` N7 `[UNKNOWN]` | Cross-tenant read/write on the module's central boundary — the ONE thing every agent this run flagged as genuinely unproven | | NOT TESTED | | | A11-M1 · tenant mutation. **This closes the single largest standing `[UNKNOWN]` in the whole certification — prioritize the fixture.** |
| A11-M1-05 | T1 | Admin panel | Save / optimistic lock | Two staff sessions, same template | Both sessions load the same template (same `lockVersion`). | Session A saves (succeeds, `lockVersion` advances). Session B, still holding the OLD `lockVersion`, saves a different field. | Two divergent patches | Session B gets a 409 (`E_CONFLICT`) and the merge-conflict UI, NOT a silent overwrite of Session A's change. | Implementation-derived — `Doc_template_service.php:314-332` | `01c-data-spec.md` §7 (lockVersion is read-then-compare-then-write, not a Firestore precondition) | Lost-edit silent overwrite, contradicts the module's own doc-comment guarantee | | NOT TESTED | | | A11-M1 · fresh→stale record |
| A11-M1-06 | T1 | Admin panel | Publish | Staff (manage) | A proofed, unpublished draft. | Click Publish. Immediately click Publish again (before the first response returns) — or send the identical POST twice back-to-back. | Two near-simultaneous `publish` POSTs | Exactly ONE version freezes. The second call either 422s cleanly ("already exists") or is blocked client-side; it must NOT create a second `_v{n}` doc or corrupt the head's `version`/`publishedVersion` pair. | Implementation-derived — `Doc_template_service.php:514-522` (create-only `exists()` guard) | — | N2 (create-only snapshots) | A double-submit under real latency could interleave the two non-atomic writes in `publish()` in an unintended order | | NOT TESTED | | | A11-M1 · single→duplicate request |
| A11-M1-07 | T2 | Admin panel | Lifecycle | Staff (manage) | A template that is `status=archived` AND `publishedVersion≠null` (archive a published-but-inactive template first). | Call `activate(id)` on the archived template. | n/a | Either `activate()` refuses (archived templates should not become the live document), or, if it is allowed by design, the UI/audit trail makes the resulting hybrid state (`archived` + `activeVersion≠null`) visible and explicable — it must not be a silent, undocumented state. | Implementation-derived — `Doc_template_service.php:600-609` (no `status` check found) | `02-state-machine.md` I-9 | An archived, "tidied away" template is, simultaneously, the one every print point would resolve to | | NOT TESTED | | | A11-M1 · valid→invalid state transition |
| A11-M1-08 | T2 | Admin panel | Publish / validation | Staff (manage) | A template whose `objects` reference merge-field keys valid under its ORIGINAL `docType`'s contract. | Follow A11-M1-02's steps 1-3 (docType swap laundered through publish), then open the resulting published version's proof/preview. | n/a | If the old and new contracts do not share the referenced keys, the render should fail loudly (unresolved-field error) rather than silently omitting or mis-binding content into the wrong document type's layout. | Implementation-derived — `01b-backend-spec.md` §6 ("not verified whether every combination of old/new contract key sets would actually be caught") | — | Merge-field/contract integrity under a `docType` mutation | A frozen record could describe itself as one document type while its content silently references another's fields | | NOT TESTED | | | A11-M1 · expected→reversed order (validate AFTER the mutation that should have been checked before it) |
| A11-M1-09 | T2 | Admin panel | Save / capacity | Staff (edit) | Any draft. | `save()` a patch whose `objects` array has 5,000 entries (or as large as the client will construct). | 5,000-object `objects` array | Server either accepts and later degrades gracefully (documented limits) or rejects with a clear "too large" error — it must not silently truncate, silently accept unbounded growth, or fail with a raw 500 that leaks internals. | Implementation-derived — `01b-backend-spec.md` §7 ("no size cap on `objects` — confirmed absence") | `04-parity-matrix.md` Axis 4 (absent both sides) | Unbounded write size, bounded only by external PHP/Firestore limits nobody has measured | | NOT TESTED | | | A11-M1 · normal→extreme dataset |

---

## M2 · Exploratory (what a real clerk actually does)

| Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status | Evidence Level | Device/Build | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A11-M2-01 | T1 | Admin panel | Publish | Staff (manage) | A fresh, never-proofed draft. | Open the template. Click Publish immediately, without ever clicking "Render proof". | n/a | A clear, specific refusal naming the missing step ("render a proof before publishing"), not a generic error. | Implementation-derived — `Doc_template_service.php:487-495` | — | N4 | Confirms the message a real clerk would actually read is the right one, not a raw exception string (per `05c-ux.md` §6, these four refusal strings ARE sent verbatim to the browser) | | NOT TESTED | | | A11-M2 · a clerk who skips a step |
| A11-M2-02 | T2 | Admin panel | Save / idle | Staff (edit) | Open a template, make an edit (autosave pending). | Leave the tab open and idle for several hours (session/claims may expire in that window). Return and trigger a save (edit one more field). | n/a | Either the save succeeds normally, or the user is told plainly their session expired and asked to re-authenticate — NOT a save that silently fails and leaves "Unsaved changes" indefinitely with no explanation. | Implementation-derived — `01a-web-spec.md` §6 (flagged `[UNKNOWN]` — "whether a `lockVersion` held across a very long idle period behaves differently... outside static reach"); `05c-ux.md` §5 (save-failure path never auto-retries) | — | Feedback honesty | A clerk who steps away mid-edit has no way to know their work stopped saving | | NOT TESTED | | | A11-M2 · abandoned work / idle-then-act |
| A11-M2-03 | T1 | Admin panel | Delete | Staff (manage) | A never-published draft. | Click "Delete draft". While the confirm modal is open, click the Delete button inside it twice in rapid succession (double-click). | n/a | The template is deleted exactly once; the second click either no-ops cleanly or the button disables after the first click — no error, no attempt to delete an already-gone document that surfaces as a scary failure. | Implementation-derived — `Doc_template_service.php:786-820` | — | Idempotency of `delete()` | A rapid double-click on a destructive confirm button is an extremely common real-world action | | NOT TESTED | | | A11-M2 · repeated action |
| A11-M2-04 | T2 | Admin panel | Navigation | Staff (any) | On the hub. Navigate hub → gallery → open a template (in-app clicks only, never typing a `/design/{id}` URL). | Press the browser Back button. | n/a | Document what actually happens (there is no in-app history — `history.pushState` is never called, `01a-web-spec.md` §1) — Back should not silently discard unsaved work or land somewhere confusing without at least a beforeunload-style warning if edits are dirty. | Implementation-derived — `01a-web-spec.md` §1 (0 `pushState`/`popstate` occurrences) | — | Navigation integrity | Browser Back leaves the SPA entirely, with no confirmation if work is unsaved | | NOT TESTED | | | A11-M2 · back navigation |
| A11-M2-05 | T2 | Admin panel | Concurrency / stale screen | Two staff sessions | Session A opens template X (draft v7, active v6). Session B, in parallel, proofs+publishes+activates v7 (now active v7), all while Session A never reloads. | Session A, still looking at its stale screen (still showing "active: v6"), makes an edit and saves. | n/a | Session A's save must not silently corrupt or misdescribe the now-different reality; the UI should reflect that the world moved (ideally via the existing 409/lockVersion path) rather than let A act on stale assumptions with no signal. | Implementation-derived — presence is explicitly "advisory... never interrupt the work" (`designer.js:1223`); the only real enforcement is the save-time 409 | — | Feedback honesty under real multi-user use | The gap between "presence shows who's here" and "presence tells you what changed" is exactly where a clerk gets surprised | | NOT TESTED | | | A11-M2 · stale screen acted upon |
| A11-M2-06 | T1 | Admin panel | Create | Staff (edit) | On the hub, hub shows document types. | Click "New custom document type". Type a name into the `askName` modal. Cancel/close the modal WITHOUT confirming. | Partial name typed, then cancelled | No `documentTemplates` document is created — cancelling must be a true no-op, not an abandoned draft left behind. | Implementation-derived — `create()` is only called from `askName`'s confirm path (`designer.js:2370-2380`) | — | No orphaned state from an abandoned action | A cancelled creation flow should leave zero trace, given the gallery already has no bulk-cleanup tooling for accumulated junk (`R-3`, `02-state-machine.md`) | | NOT TESTED | | | A11-M2 · abandoned/interrupted flow |
| A11-M2-07 | T2 | Admin panel | Proof render | Staff (edit) | A draft ready to proof. | Click "Render proof". Immediately close the browser tab (before the response returns). | n/a | The server-side render either completes and records `lastProof` cleanly, or fails and records nothing — there must be no partially-written PDF file on disk and no `lastProof` record whose `hash` doesn't match what's actually on disk. | Implementation-derived — `Doc_templates.php:857-867` writes the file, then computes an aggregate hash from in-memory bytes, then calls `recordProof()` — three separate steps with no visible transaction wrapping them | — | Proof-record/file consistency | An interrupted client does not stop the server-side PHP request; the file write and DB write could still land in an inconsistent order relative to what the human ever saw | | NOT TESTED | | | A11-M2 · interrupted flow (tab-close, not client-visible) |

---

## M3 · Chaos (network / timeout / session / 5xx / concurrency-under-failure)

| Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status | Evidence Level | Device/Build | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A11-M3-01 | T0 | Admin panel | Publish | Staff (manage) | A proofed draft, instrumented environment where the connection to Firestore can be killed between two specific calls. | Trigger `publish()`. Kill the network/process AFTER the `documentTemplateVersions` write lands but BEFORE the `documentTemplates` head update completes. | n/a | The template enters the documented STRANDED state (§ `02-state-machine.md`). Confirm: (a) the snapshot exists and is correct; (b) every retry of `publish()` refuses with "already exists"; (c) there is NO way to recover except the undesigned `version`-bump side-effect (I-10) — and if AR1 (`05-risk-register.md`) is fixed by stripping `version`, this row must be re-run to confirm a REAL repair tool now exists, because the accidental escape hatch will be gone. | Implementation-derived — `Doc_template_service.php:514-522,551,563` | `05-risk-register.md` AR3, U2 | Permanently stuck template, no self-service recovery | | NOT TESTED | | | A11-M3 · timeout mid-publish. **Re-run mandatory after AR1's fix ships.** |
| A11-M3-02 | T1 | Admin panel | Save | Staff (edit) | Mid-edit, session/claims about to expire (or forcibly expired via an admin action in another tab). | Let the session expire. Attempt a Save. | n/a | A clear "please log in again" failure — not a save that appears to succeed (phantom success) and not a save that fails silently with the status bar still reading "Unsaved changes" with no explanation of why. | Implementation-derived — session/claims expiry interaction with `lockVersion` flagged `[UNKNOWN]` by `01a-web-spec.md` §6 | — | Feedback honesty | | | NOT TESTED | | | A11-M3 · session expiry mid-flow |
| A11-M3-03 | T1 | Admin panel | Save | Staff (edit) | Mid-autosave. | Force a 500 from the server during an autosave cycle (e.g., a malformed Firestore write, or use a network proxy to inject a 500). | n/a | The status bar must show the failure honestly (not silently retry-forever with no visible state, and not report success). `S.dirty` must remain `true`. | Implementation-derived — `05c-ux.md` §5 ("the save-failure path never retries on its own... the draft sits in 'Unsaved changes' indefinitely") | — | Feedback honesty | Confirmed defect shape already flagged by A10; this row reproduces it | | NOT TESTED | | | A11-M3 · server 5xx |
| A11-M3-04 | T0 | Admin panel | Proof render | Staff (edit) | Ready to proof. | Click "Render proof". Kill the browser tab (or the underlying PHP process, if testable) mid-render, specifically between the mPDF render completing and `file_put_contents()`/`recordProof()` completing. | n/a | No partially-written (truncated/corrupt) PDF is left at the version path that a subsequent `version_pdf()` could serve to a viewer as if it were complete. | Implementation-derived — `Doc_templates.php:857-867`, no atomic-write pattern (no write-to-temp-then-rename) found | — | Artefact integrity | A truncated PDF silently served as "the certificate" is a worse failure than an honest error | | NOT TESTED | | | A11-M3 · tab killed mid-render |
| A11-M3-05 | T1 | Admin panel | Publish | Staff (manage) | Same as A11-M1-06, restated under network-chaos framing. | Double-click Publish under a slow/throttled connection (Chrome DevTools network throttling) so both requests are genuinely in flight simultaneously, not just fast sequential clicks. | n/a | Exactly one version freezes; no corrupted head state (`version`/`publishedVersion` mismatched or a stray orphaned `documentTemplateVersions` doc). | Implementation-derived — same as A11-M1-06 | — | N2 | This is the TRUE concurrent case A11-M1-06 could only approximate — run both | | NOT TESTED | | | A11-M3 · double-click under real latency |
| A11-M3-06 | T2 | Admin panel | Timestamps | Staff (any) | Client machine's clock set far ahead or behind actual time. | Perform any write (save, publish, proof). | n/a | All stored timestamps (`createdAt`, `updatedAt`, `publishedAt`, `renderedAt`) reflect SERVER time, not the client's skewed clock — `Doc_template_service::now()` should be server-authoritative by construction; confirm this holds in practice, not just by code inspection. | Implementation-derived — `Doc_template_service.php` `now()` helper (server-side PHP, not client-supplied) | — | Audit-trail integrity | Low risk if `now()` truly never accepts a client value — worth one confirmation pass given the trail is legally framed | | NOT TESTED | | | A11-M3 · clock skew |
| A11-M3-07 | T1 | Admin panel | Session scoping | Staff (any) | A template created in academic session S1. | Roll the school's active academic session forward to S2 (or simulate via whatever session-switch mechanism the panel uses elsewhere). Reopen the Document Engine hub/gallery. | n/a | Document what actually happens: the template is EXPECTED to still appear (this collection is deliberately NOT session-scoped per `04-parity-matrix.md` Axis 2 — a real, confirmed deviation from `CLAUDE.md`'s "every query is scoped twice" rule) — confirm this is intentional product behaviour and not an oversight, since every other module in this codebase treats missing session-scoping as its single most repeated bug. | Implementation-derived — `01c-data-spec.md` §1a (`documentTemplates` conforms to `{schoolId}_{entityId}`, not session-scoped) | `qa/_patterns.md` "Missing academic-session filter" | A template designed and correct for session S1 (e.g. referencing a state rule or fee schedule that changed) persists unchanged into S2 with no review gate | | NOT TESTED | | | A11-M3 · academic-year rollover |
| A11-M3-08 | T1 | Admin panel | Concurrency | Two staff sessions, same template | Tab A and Tab B both open the same template. | Tab A saves an edit (succeeds, server `lockVersion` advances). Tab B, unaware, immediately tries to Publish using its own (now-stale) in-memory `S.tpl`. | n/a | `publish()` must operate on the SERVER's current head (it does — confirmed, `Doc_template_service.php:483` re-reads via `head()`), so Tab B's publish should either succeed against the CURRENT (Tab-A-edited) content, or correctly refuse if Tab B never re-proofed after Tab A's edit invalidated the proof (`contentHash` mismatch). Confirm Tab B is never allowed to publish content it displayed but the server no longer holds. | Implementation-derived — `Doc_template_service.php:483,508-513` | N4 | Two tabs on one template is explicitly named as a target scenario in the mission | | NOT TESTED | | | A11-M3 · two tabs, one template |

---

## M4 · Boundary (null / extreme / Unicode / RTL / whitespace / scale)

| Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status | Evidence Level | Device/Build | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A11-M4-01 | T2 | Admin panel | Custom type minting | Staff (edit) | Hub, "New custom document type" flow. | Type a custom-type title that is only punctuation, e.g. `!!!___###···` (no letters/digits at all). | `"!!!___###···"` | Client and server must AGREE on the outcome (both reject, or both accept the same derived id) — per QA-LEAD's own `İ`-class finding (`_live-state.md` L12), this is the exact SHAPE (client/server slug-derivation disagreement) most likely to diverge again on a new boundary input. Do NOT repeat the `İ` case itself — this is a distinct input (punctuation-only, collapses to an empty slug on both sides by the collapse regex, but confirm the REJECTION MESSAGE/behavior matches, not just the end id). | Implementation-derived — `Doc_contract.php:155-170` vs `designer.js:1564-1568`, both collapse non-`[a-z0-9]` runs, both should produce `""` for pure punctuation | `04-parity-matrix.md` Axis 1 | Same-name-different-mechanism divergence (PHP throws with a message, JS silently no-ops — already noted as a MECHANISM divergence even where the END RESULT agrees) | | NOT TESTED | | | A11-M4 · punctuation-only name (extends, does not repeat, QA-LEAD's `İ` boundary class) |
| A11-M4-02 | T2 | Admin panel | Template naming | Staff (edit) | Any draft. | Set the template's crumb/name field to a 10,000-character string. | 10,000-char name | Document the actual outcome — there is NO length cap found on either side (`01a-web-spec.md` §7). Confirm whether this silently truncates somewhere downstream (Firestore field limits, UI rendering), fails the write outright, or is genuinely accepted and stored in full — and if accepted, confirm it doesn't break the gallery-row rendering for every OTHER user who has to look at it. | Implementation-derived — `01a-web-spec.md` §7 ("CLIENT-ONLY / UNBOUNDED... no length cap at all, client or found server-side") | — | Absence of a bound, both sides | | | NOT TESTED | | | A11-M4 · 10k-char string |
| A11-M4-03 | T2 | Admin panel | Object geometry | Staff (edit) | A template with at least one object. | Set an object's `x`/`y`/`w`/`h` (mm) to an extreme value — `-99999` and separately `1e9`. | `-99999`, `1000000000` | `evalMm()` rejects non-numeric input but performs NO range clamp (`designer.js:1651-1661`), and no server-side bound exists either (`01b-backend-spec.md` §7). Confirm the actual failure mode: does the canvas silently render an off-page/invisible object, does `proof_pdf()` time out or OOM against `Doc_renderer::MAX_MEMORY`/`MAX_SECONDS` (`Doc_renderer.php:38-40`), or does mPDF throw a clean, catchable error? | Implementation-derived — `04-parity-matrix.md` Axis 4 (client-only, non-range) | — | Render-time resource bounds under attacker-controlled geometry | Ties directly to AR6's sibling render-time resource risk (upload decompression) — this is the geometry-side analogue, previously untested by any agent | | NOT TESTED | | | A11-M4 · negative/extreme geometry |
| A11-M4-04 | T2 | Admin panel | Multilingual render | Staff (edit) | A template with `languages` including one RTL/Indic script. | Set a text object's content to a mix of Devanagari, an RTL script (if supported), and emoji in one string, on one line. Render a proof. | Mixed-script + emoji string | The PDF renders without corruption, crash, or silent character loss — and does not crash `proof_pdf()`'s font-manifest resolution for a bilingual certificate (the module's own stated purpose, per `doc_types.php`'s multilingual design). | Implementation-derived — `languages`/`defaultLanguage` are core to the design (`01c-data-spec.md` §1b); no prior agent exercised a genuinely mixed-script boundary case | `qa/_patterns.md` (no direct citation — new boundary class) | Rendering correctness for the module's actual target use case (Indian statutory certificates, multiple scripts) | | NOT TESTED | | | A11-M4 · RTL/Devanagari + emoji mixed |
| A11-M4-05 | T2 | Admin panel | Canvas scale / render cap | Staff (edit) | A template built or scripted to hold 500 objects on one page. | Open it in the designer (type one character to trigger `layoutPage()`). Separately, call `proof_pdf()` on it. | 500 objects | Designer: document the actual keystroke-to-repaint latency (no throttling exists per `05b-performance.md` §6a — `layoutPage()` runs full O(objects) rebuild + forced-reflow on every keystroke). Proof: confirm `Doc_renderer::MAX_PAGES=20`/`MAX_SECONDS=15` (`Doc_renderer.php:38-40`) produce a CLEAN, user-facing error rather than a raw 500 or a silently-truncated PDF. | Implementation-derived — `05b-performance.md` §6a, §4 | — | Perf ceiling + clean failure mode at the documented cap | The `tc_p95.html` golden fixture exists (`00-dependency-graph.md` §1) but was never opened/measured by any agent this run — this row is the first to actually exercise it | | NOT TESTED | | | A11-M4 · 500 objects, one page |
| A11-M4-06 | T2 | Admin panel | Naming | Staff (edit) | Hub / any draft. | Set a template name to whitespace-only (`"   "`, various Unicode whitespace). Attempt to mint a custom type with a whitespace-only title. | `"   "` (various whitespace characters) | Template name: falls back to "Untitled template" per the empty-check (`designer.js:2087`) — confirm whitespace-only is actually caught by that check (a naive `!value` check would NOT catch `"   "`). Custom type: the slug-collapse should reduce whitespace-only to an empty slug and be refused the same way punctuation-only is (A11-M4-01) — confirm it doesn't instead collide with another whitespace-derived template's id. | Implementation-derived — `designer.js:2087` (empty-string check specifically, not a trim-aware one, per naive reading) | — | No silent id collision from a degenerate name | | | NOT TESTED | | | A11-M4 · whitespace-only |

---

## M5 · Concurrency (the module's own weakest architectural seam)

| Test ID | Tier | Surface | Module Area | Role | Preconditions | Steps | Input | Expected Result | Expectation Source | Evidence | Invariant Defended | Risk | Actual Result | Status | Evidence Level | Device/Build | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A11-M5-01 | T0 | Admin panel | Save / CAS | Two staff sessions, same template, same `lockVersion` | Both sessions have loaded the template and hold the identical `lockVersion`. | Fire both sessions' `save()` calls as close to simultaneously as tooling allows (parallel scripted requests, not sequential UI clicks), with DIFFERENT patches. | Two divergent patches, same `lockVersion` | Per the module's own doc-comment (`Doc_template_service.php:27-28`: "the loser gets a conflict; nobody gets a lost edit"), ONE request should be refused. **This is the direct test of C1** (`_arbitration.md`) — the code is a plain PHP read-then-compare-then-write with NO Firestore precondition (`01b-backend-spec.md` §5); this row is what would prove or disprove real-world exploitability under true concurrency, which no static pass can settle. | Implementation-derived — `Doc_template_service.php:314-332` | `_arbitration.md` C1 | Both writes could succeed, with the second silently discarding the first's fields — the doc-comment's guarantee failing exactly as C1 predicts | | NOT TESTED | | | A11-M5 · same `lockVersion`, true concurrency. **The single highest-value concurrency row — settles C1.** |
| A11-M5-02 | T0 | Admin panel | Create / numbering | Two staff sessions, same school | Both sessions on the hub, ready to create a new template of the same type. | Fire both sessions' `create()` calls simultaneously (scripted, not sequential clicks). | n/a | Two DISTINCT `TPL####` ids are minted. If both compute the same `$max` and both pass `exists()` before either `set()`, the second `set()` silently overwrites the first template with no error (`01b-backend-spec.md` §4's own predicted failure mode). | Implementation-derived — `Doc_template_service.php:194,206,245` (unbounded query + TOCTOU exists/set, no precondition) | — | No template is silently annihilated by a same-tick sibling create | | | NOT TESTED | | | A11-M5 · two creates at once (id race) |
| A11-M5-03 | T0 | Admin panel | Publish | Two `manage` sessions, same template | Both sessions have the same proofed draft open. | Fire both sessions' `publish()` calls simultaneously. | n/a | Exactly one version freezes cleanly; the loser gets a clean refusal (ideally "already exists" or a conflict), not a corrupted head (`version`/`publishedVersion` out of sync with which snapshot actually exists). | Implementation-derived — same as A11-M1-06/A11-M3-05, this row is the TRUE-concurrent variant | — | N2 | | | NOT TESTED | | | A11-M5 · publish twice simultaneously |
| A11-M5-04 | T1 | Admin panel | Activate | Two `manage` sessions | Two DIFFERENT published templates of the SAME `(school, docType)`, both being activated in the same tick by two different staff (e.g., two people independently decide "this one should be live"). | Fire both `activate()` calls simultaneously, each activating a different template. | n/a | Exactly ONE template ends up active — `activate()`'s `commitBatch` with `precondition:{exists:true}` should make this genuinely atomic (`03-invariants.md` N1) — this row is the runtime confirmation of a claim currently proven only by code trace. | Implementation-derived — `Doc_template_service.php:650-702` | N1 | This is the one place the module claims REAL atomicity — it deserves the same concurrency scrutiny as the seams that don't | | | NOT TESTED | | | A11-M5 · activate while another activates |
| A11-M5-05 | T1 | Admin panel | Save vs. publish race | Two sessions, same template | Session A mid-edit (dirty, autosave pending). Session B has the current published draft ready and clicks Publish. | Fire Session A's autosave and Session B's `publish()` as close together as possible. | n/a | Session B's publish must not freeze content that was mid-write from Session A's still-in-flight save (a torn read) — and Session A's subsequent save must not land ON TOP of the just-published next-draft in a way that silently corrupts the new version numbering. | Implementation-derived — no cross-endpoint locking exists between `save()` and `publish()`, each is its own independent `head()` read | — | Cross-endpoint consistency the module has never been tested for | | | NOT TESTED | | | A11-M5 · edit while a colleague publishes |
| A11-M5-06 | T1 | Admin panel | `save_block` | Two staff sessions, DIFFERENT schools (School A, School B) | Neither school has yet created a `reusableBlocks` document with the id both are about to use. | Both sessions POST `save_block` with the IDENTICAL `blockId` (e.g. `"letterhead"`) within the same narrow window, via direct HTTP (this endpoint has zero UI callers, so this must be scripted). | Identical `blockId`, different `schoolId` (forced server-side per session) | The LOSING school's client must be told it lost — a conflict, not a silent 200. Today, per code trace, BOTH callers receive a clean success response and the second write's `schoolId` silently wins (`Doc_block_service.php:94-105`, no CAS). This row directly tests `05-risk-register.md` AR4. | Implementation-derived — `Doc_block_service.php:100-105` (TOCTOU: only refuses on an EXISTING doc with a DIFFERENT stored `schoolId`; a first-write race is unguarded) | `05-risk-register.md` AR4, `05a-security.md` §1b/§3 | A school's own letterhead/seal/signature block is silently reassigned to another tenant, and the true owner is told "Saved" | | NOT TESTED | | | A11-M5 · `save_block` id collision across schools. **Currently reachable only via direct HTTP (no UI path) — note this narrows the realistic actor to a technical insider, but does not close the gap.** |
| A11-M5-07 | T2 | Admin panel | Activate / rollback | Two `manage` sessions, same template | `publishedVersion` history has at least 3 versions. | Session A activates the NEWEST version. Session B, simultaneously, activates an OLDER version (rollback) on the same template. | Two different target versions, same template, same tick | The final `activeVersion` deterministically reflects whichever `commitBatch` actually landed last at the Firestore layer — not a mixed/inconsistent state, and both sessions' UI should reflect the TRUE final state on their next read, not each session's own optimistic assumption. | Implementation-derived — `Doc_template_service.php:600-725`, atomic per-call but two INDEPENDENT atomic calls racing each other | — | N1 under a rollback-vs-forward race specifically | Narrower than A11-M5-04 (same template, opposite direction) — a distinct interleaving worth its own row | | NOT TESTED | | | A11-M5 · rollback vs. forward-activate race |

---

## Counts

| Method | T0 | T1 | T2 | T3 | Total |
|---|---|---|---|---|---|
| M1 Mutation | 2 | 4 | 3 | 0 | 9 |
| M2 Exploratory | 0 | 3 | 4 | 0 | 7 |
| M3 Chaos | 2 | 4 | 2 | 0 | 8 |
| M4 Boundary | 0 | 0 | 6 | 0 | 6 |
| M5 Concurrency | 3 | 3 | 1 | 0 | 7 |
| **Total** | **7** | **14** | **16** | **0** | **37** |

No T3 (surface/polish) rows were authored this pass — every scenario found either touches a
T0/T1 invariant directly or is a T2 boundary/robustness question; polish-only findings belong
to A10, not to an adversarial pass. If A12 wants T3 coverage of the modal-focus-bleed defect
(`05-risk-register.md` AR11) specifically as a UX row rather than a security row, that
belongs in A10's sheet, not duplicated here.
