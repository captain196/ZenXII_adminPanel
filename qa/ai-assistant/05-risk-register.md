# 05 — RISK REGISTER · AI Assistant ("Ask ZenXii")

**Agent:** A12 · TEST-ARCHITECT · reports to QA-LEAD
**Date:** 2026-08-31 · **Evidence ceiling: E2** — every entry is a static-source finding. Nothing
below has been executed. Severity is the risk *if the code behaves as read*.

**Scope of this register.** Eighty distinct risks, deduplicated across the ten specialist reports and
re-graded against A11's attack results and QA-LEAD's arbitration. Where several agents found the same
thing it appears **once**, with all owners cited. Every row names the UAT rows in
`06-uat-matrix.csv` that would settle it.

**Three re-grades from QA-LEAD are binding and are applied throughout:**
1. A9's "33–99× over cost target" is **OVERTURNED** — the real figure is ~10× against the
   `~₹3/student/month` target the code itself states, and the correct framing is *"the 30/day quota
   is a fair-use control, not a cost control."*
2. "The feature has never run in production" is **DOWNGRADED to `[UNKNOWN]`** (R-01b).
3. A7's C1 (sibling switch) is **correctness/identity, not authorization** — G1 and G2 are intact.

---

## P0 — certification blockers

| ID | Surface | Symptom (one line) | Root cause | Invariant at risk | Citation | UAT rows |
|---|---|---|---|---|---|---|
| **R-01** | Cloud Function | The per-school kill switch has no writer anywhere in the ecosystem, so no school can opt in and no consent is ever recorded | `ai_assistant_enabled` is read at one site and written at none; `School_config::save_profile` writes through an `$allowed` whitelist that blocks incidental writes | Feature reachability + consent attributability (E-B2) | `studentAssistant.js:158`; `School_config.php:383,401`; `index.js:669` | T0-01, T0-02 |
| **R-01b** | Firestore | *Consequence* claim "therefore the feature has never run" — **DOWNGRADED to `[UNKNOWN]`** | Repo-absence does not entail production-absence: console, Admin SDK, the A4 write-capable credential and unversioned scripts all leave no repo trace | Evidence integrity | A11 Attack 2 | **T0-02** |
| **R-02** | Cloud Function | `get_attendance_summary` addresses a document id that has never been written, and the prompt turns the miss into a confident "there is nothing recorded" logged as `ok:true` | The tool interpolates a `"Month YYYY"` label into a key whose canonical form is `{schoolId}_{studentId}_{YYYY-MM}`; the normaliser exists in the Parent app and the CF does not call it | I3, I9 | `studentAssistant.js:291-295,428-433,229,458`; `Attendance.php:912,1305`; `AttendanceFirestoreRepository.kt:69-77` | T0-13, T0-14, T0-15, T1-10, T1-69 |
| **R-03** | Cloud Function | `get_exam_results` returns a real grade and a real maximum beside a **null mark** | Three of six projected fields (`subject`, `marksObtained`, `published`) do not exist on a `results` document; `subjects{}` is dropped entirely | I9 | `studentAssistant.js:374-384`; `Exam_result_store.php:157-180`; `ResultDoc.kt:11-38` | T0-28, T1-14, T1-71 |
| **R-04** | Cloud Function | The system prompt orders the model to announce a ticket the tool has not filed since 2026-08-30 | `:443` and `:467` still describe the pre-rework behaviour while the tool description at `:271` and its guidance at `:420-423` forbid exactly that — the same payload carries both | **I2** | `studentAssistant.js:271,388-425,443,467` | T0-22, T1-15, T1-68 |
| **R-05** | Parent Android | The assistant says it prepared a support request; the app discards the draft and shows an empty form | The CF returns five handoff fields; the client reads two; the route takes no arguments; the compose screen accepts no prefill | I2 (a claim the app cannot honour) | `studentAssistant.js:586-593,421-423`; `AssistantRepository.kt:57-62`; `NavGraph.kt:200,843`; `SupportComposeScreen.kt:51-55` | T0-23, T1-26 |
| **R-06** | Cloud Function | Thirty empty payloads burn a child's whole daily allowance with zero model calls and zero log rows | `consumeQuota` commits seven lines before the empty-message check; no compensating write exists in `functions/` | **I6** | `studentAssistant.js:483,489-490,201-207` | **T0-19**, T2-44 |
| **R-07** | Cloud Function | One Vertex or IAM fault burns every active student's full day, with no audit record of what happened | `generateContent` is an unwrapped `await`; the throw propagates past both `writeLog` sites | I6, I7 | `studentAssistant.js:534,483,557,607` | T0-42, T2-45, T2-69 |
| **R-08** | Cloud Function | `get_timetable` merges every session's periods and can return only expired ones | No `session` filter and no `orderBy`; docIds sort session-ascending so the oldest sorts first, and `QUERY_LIMIT = 25` can fill entirely from expired sessions | **Z3** | `studentAssistant.js:17-19,355-359,85`; `TimetableDoc.kt:9` | T0-31, T2-34, T1-72 |
| **R-09** | Parent Android | A capability change retroactively deletes content: the unavailable state destroys every answer already on screen and never clears | `unavailableReason` has one writer and no clearing path; the screen early-returns before the message list, the chips and the composer are composed | **I10** | `AssistantViewModel.kt:22,96`; `AssistantScreen.kt:99-102` | T0-41, T0-44, T2-14, T2-80, T2-81, T2-82, T2-83 |
| **R-10** | Parent Android | A claims fault is reported as "your school hasn't enabled this" and permanently bricks the screen | `PERMISSION_DENIED` is folded in with `FAILED_PRECONDITION` with no stated rationale; two machines with different owners collapse into one absorbing state | I10, Z1 | `AssistantViewModel.kt:78-85,90-99`; `studentAssistant.js:132-134,172-174` | T0-41, T2-81 |
| **R-11** | Parent Android | A self-harm referral renders identically to a fee-balance answer and the helpline is not tappable | The Tele-MANAS number exists only inside model prose; `isError` is false for a refusal so no alternate styling applies; no `LinkAnnotation`, no `autoLink`, no `ACTION_DIAL` anywhere | Child-safety duty of care | `studentAssistant.js:447`; `AssistantScreen.kt:175-179,189-199`; zero `14416` in `app/src/main` | T0-34, T3-26, T3-27 |
| **R-12** | Cloud Function | Staff-authored homework free text carries other pupils' names straight into a child-facing prompt, uncapped | `get_homework` returns `description` and `title` verbatim, 25 rows deep, and `:473` tells the model to treat retrieved text as ordinary content **to report** | **I1** (catalogue wording; the structural core holds) | `studentAssistant.js:318-326,473,85` | T0-24, T2-66 |
| **R-13** | Cloud Function | A stale `className`/`section` returns a **full, plausible, wrong** result set from another section | `sectionKey` is composed from the student doc with no validation; the code's own comment warns only about the *empty* failure mode | I1 residual (data outside entitlement) | `studentAssistant.js:110,111-113,180-181,308,354` | T0-26 |
| **R-14** | Firestore | Children's free-text questions — including the wellbeing disclosures the prompt solicits — accumulate permanently with no reader, no TTL, no export and no erasure | `writeLog` persists 500 chars of `question`; no TTL field, no `onSchedule` sweep, absent from `Data_service`'s school-scoped list, zero readers in four repos | Retention/minimisation (**no G or Z invariant covers it** — itself a finding) | `studentAssistant.js:620-638,627,447`; `Data_service.php:344-346` | T0-39, T2-06, T2-58, T2-59, T2-74 |
| **R-15** | Cloud Function | Every fee row reaches the model with `amount: null` on the one topic the prompt calls sensitive | `f.amount` has no source field; the writer emits `grossAmount`/`discountAmount`/`netAmount` | I9; G4-adjacent | `studentAssistant.js:342,462`; `Fees.php:4795-4797`; `Fee_generation_engine.php:607-613` | T0-29, T1-12, T1-70 |

---

## P1

| ID | Surface | Symptom | Root cause | Invariant at risk | Citation | UAT rows |
|---|---|---|---|---|---|---|
| **R-16** | CF callable | Any legitimate credential holder can author the assistant's side of the conversation | History is filtered on type and role **label** only; `assistant` and `model` both map to Gemini's authoritative `model` role; no signature, no server transcript, no turn counter | **I4** — unenforceable by construction | `studentAssistant.js:485-513,506-511` | T0-03, T0-38, T2-53, T2-54 |
| **R-17** | Cloud Function | The assistant over-states what a family owes | `get_fee_status` applies no `status` filter; the app excludes `archived` server-side in all three read paths | Business rule (app authoritative) | `studentAssistant.js:331-336,346`; `FeeFirestoreRepository.kt:89-97` | T0-30 |
| **R-18** | Parent Android + CF | After a sibling switch the assistant names and answers about the **wrong child**, confidently and silently | `switchToSibling` never touches FirebaseAuth; `student_id` is a property of the credential, so no client-side refresh can fix it and no endpoint re-mints a claim for a sibling | G2 **intact** — correctness/identity | `DashboardViewModel.kt:259-328`; `studentAssistant.js:126,496`; `Firebase.php:855-865` | **T0-18**, T0-05, T2-49 |
| **R-19** | Parent Android | "Answered-into-the-void": billed, logged `ok:true`, never delivered, and the student is blamed for their question | The callable is invoked with no `.withTimeout` against a 120 s server budget; the Android SDK default is 70 s | I7 | `AssistantRepository.kt:41-44`; `studentAssistant.js:479,483,557` | T0-08, T1-61, T2-45 |
| **R-20** | Cloud Function | One logical question can produce two decrements, two log rows and two billed model calls | No `requestId`, nonce or dedupe key is accepted; `assistantLogs` uses `.add()` with an auto-id; the client guard is a main-thread accident | **G5** | `studentAssistant.js:486-490,193,622`; `AssistantViewModel.kt:39` | T0-21, T2-46, T2-55 |
| **R-21** | Cloud Function | Silent truncation at 25 rows on all five read tools, with no `truncated` flag, so the model states confidently wrong totals | `QUERY_LIMIT = 25` bounds rows; nothing tells the model it saw a partial set | Business rule (money, in the fees case) | `studentAssistant.js:85,462` | T2-31, T2-32, T2-33, T2-34 |
| **R-22** | Cloud Function | `get_timetable` ships whole raw documents — `teacherId`, `room`, `updatedAt` — into a child-facing prompt | It is the only tool of five with no field projection | **I8** | `studentAssistant.js:361` vs `:297-303,318-326,338-348,374-384`; `TimetableDoc.kt:13-38` | T0-32, T2-35 |
| **R-23** | Cloud Function | The quota resets 5½ hours after the reset both surfaces promise; and one IST calendar day spans two buckets (up to 60 questions) | The bucket is keyed on `new Date().toISOString().slice(0,10)` on a UTC runtime | **I11** | `studentAssistant.js:192,198-199`; `strings_assistant.xml:40` | T2-12 |
| **R-24** | Cloud Function | Between 00:00 and 05:29 IST the model is told **yesterday's date** and reasons about due-today/overdue against local date strings | Same UTC root, second site: the per-request context line | I11 | `studentAssistant.js:498`; `Fee_generation_engine.php:580` | T2-13, T2-67 |
| **R-25** | CF callable | An authenticated caller sizes the token cost of one quota unit; the quota counts requests, not tokens | No length cap on `message` or on any history `content`, while the *model's* own strings are capped at 200/2000 in the same file | The cost model at `:189` | `studentAssistant.js:489,487,406-407` | T2-24, T2-25 |
| **R-26** | CF callable | No App Check anywhere in the project: a costed, model-backed endpoint has no client-integrity signal | `onCall` sets only region/timeout/memory; grep for `enforceAppCheck` across `functions/*.js` returns nothing | — (not an authz hole) | `studentAssistant.js:478-479` | T2-50 |
| **R-27** | Cloud Function | The panel's fee-defaulter result-withhold gate has no twin — the assistant is a third channel, and the most quotable | The gate reads RTDB and the CF makes no RTDB read; the flag also has three homes | **P-04** | `Result.php:307-323,347`; `studentAssistant.js:366-386` | T0-04, T0-27, T2-63 |
| **R-28** | Firestore | `assistantLogs` cannot validate `assistantQuota` in **either** direction, and the sign of the difference is not diagnostic | Duplicates over-count (R-20); swallowed writes and throw paths under-count (R-07); there is no third source | **I7** | `studentAssistant.js:557,607,622,639-641,560` | T0-40, T1-47, T2-41, T2-45 |
| **R-29** | Cloud Function | A tool failure is absorbed into a successful answer: fluent prose, HTTP 200, no chip, no log entry | The per-tool `try/catch` converts a throw into a `functionResponse` and the loop continues; `toolsUsed.push` sits inside the success branch | I7 | `studentAssistant.js:583-604` | T2-41, T1-23, T1-47 |
| **R-30** | Cloud Function | A Vertex 429 at peak becomes an unmapped `internal` after quota was burned, with no retry and no backoff | The model call sits in no `try/catch`; the Vertex client is constructed inside the handler so nothing is reused on a warm instance | I6 (quota fairness) | `studentAssistant.js:517-521,534-545`; contrast `:596-602` | T2-69, T0-42, T1-75 |
| **R-31** | Cloud Function | The whole cost model rests on a ~1,890-token prefix clearing an undocumented implicit-cache minimum, with a <10% margin | The file asserts "no minimum-prefix cliff"; the measured prefix sits between the documented 1,024 and 2,048 minima | The cost model at `:189` | `studentAssistant.js:41-46,440-473,216-286,550` | T0-06 |
| **R-32** | Cloud Function | The module's **two real controls** (I1's argument-free tool schemas, I5's write-free `TOOL_IMPL`) are each repealed by a single plausible edit, with no test, no lint and no rules block to object | No offline test of any kind; `_test` exports `TOOL_IMPL` and `TOOLS` for exactly the assertion nobody wrote; the harness the comment names does not exist | **I1 and I5** — regression defence | `studentAssistant.js:648,216-286,289-426`; `functions/package.json` (no `scripts`) | T2-76, T0-33, T0-17 |
| **R-33** | Parent Android | One entry point, behind an auto-raised keyboard, keyworded in English only — effectively unreachable in five of six locales | `Route.Assistant` has two references in the whole app; the catalogue row's title and eleven keywords are hardcoded English | — | `SearchViewModel.kt:236-238`; `NavGraph.kt:837`; `SearchScreen.kt:64` | T1-01…T1-05, T3-34 |
| **R-34** | Parent Android | After an in-app language change, all five assistant error strings render in the process-start language | `AssistantViewModel` uses `getString` on the Application context; the codebase's own rule mandates `localizedString` in every ViewModel, against a device-observed failure | — | `AssistantViewModel.kt:87,96,104,106,108,109`; `LocaleManager.kt:159-169,197-199` | T3-36 |
| **R-35** | Parent Android | The primary call-to-action is permanently English in all six locales | `handoffLabel` is always non-null from the server, so the `?:` fallback to the translated resource is dead code | — | `AssistantScreen.kt:216`; `studentAssistant.js:414-416` | T1-25, T3-35 |
| **R-36** | Cloud Function | Loop exhaustion throws away six billed model calls, every Firestore read, `toolsUsed` and any assembled handoff — and uses a code that invites a retry | `handoff` is built inside the loop and read only on the success return; `deadline-exceeded` is semantically wrong for an iteration cap | I2 (the escape hatch is destroyed) | `studentAssistant.js:531,586-594,605-610,563` | T2-38, T2-39 |
| **R-37** | Parent Android | A malformed or empty `reply` produces a blank bubble with no error and no log — the module's one silent failure | The payload is coerced with `.orEmpty()`; neither the ViewModel nor the screen checks for blank text | Fail closed and visibly | `AssistantRepository.kt:47,58`; `AssistantViewModel.kt:60-71`; `AssistantScreen.kt:189-199` | T2-26 |
| **R-38** | Parent Android | A server-supplied route string reaches `navController.navigate()` with no allowlist — an unregistered route is a main-thread crash from a tap on an AI-produced button | No validation on `handoff.route` anywhere in the chain; the graph contains parameterised routes a crafted string could target | Cross-repo string contract | `AssistantRepository.kt:60`; `AssistantScreen.kt:206-210`; `NavGraph.kt:843` | T0-45 |
| **R-39** | GCP | No spend ceiling, no `maxInstances`/`minInstances`/`concurrency`, and an unknown Vertex QPM quota | Nothing is set anywhere; `functions/index.js` has no `setGlobalOptions` | The cost model at `:189` | `studentAssistant.js:479`; `functions/index.js` | T2-70, T2-71, T2-72 |
| **R-40** | Cloud Function | The cheapest path to a records answer is the forbidden one — the transcript is replayed in full and the UI cannot signal a chip-less answer | I3 is PROMPT-ONLY; there is no negative provenance marker, and chip-absent conflates "no tool" with "tool failed" | **I3** | `studentAssistant.js:443,486-488`; `AssistantScreen.kt:165-168,227` | T1-54, T2-01, T0-37 |
| **R-41** | Parent Android | Quota exhaustion is rendered in the *retryable* costume under a fully enabled composer, so the user retries into a wall | `RESOURCE_EXHAUSTED` becomes an ordinary error bubble at 8% alpha; `enabled = !isThinking` only | — | `AssistantViewModel.kt:103-104,112-121`; `AssistantScreen.kt:121,334` | T2-78, T1-60 |
| **R-42** | Parent Android | An error carries no recovery: re-asking means retyping from memory | No `onRetry` in the screen or the ViewModel; the failed user turn stays in the transcript | — | `AssistantViewModel.kt:43-47,112-121` | T2-79 |
| **R-43** | Parent Android + CF | After any error the replayed history contains two consecutive `user` turns; if the model rejects that shape, one failure poisons the conversation permanently | `filterNot { isError }` removes the error bubble but keeps the orphaned user turn | I10 | `AssistantViewModel.kt:43-47`; `studentAssistant.js:504-513` | T0-07, T2-11 |
| **R-44** | Firestore | The feature writes its own cost telemetry into collections nobody can query, so the cache-detection path the code describes is inert | Zero declared indexes for `assistantLogs` and `assistantQuota` | Observability of the cost model | `firestore.indexes.json`; `studentAssistant.js:617-618,631-636` | T2-73, T2-74 |

---

## P2

| ID | Surface | Symptom | Root cause | Invariant at risk | Citation | UAT rows |
|---|---|---|---|---|---|---|
| **R-45** | Cloud Function | One model-authored string reaches a Firestore document path unvalidated | `input.month` receives only `String().trim()` before interpolation; the JSON schema constrains the model, not the server | Contract #2 ("no model-authored queries"). **G1/G2 survive** — the tenant+student prefix is immutable and path segments admit no `..` | `studentAssistant.js:15-17,291-294,584` | T2-36 |
| **R-46** | Cloud Function | `handoff.category` is never validated against its own declared enum | Coerced with `String(input.category \|\| 'other')`, no membership test | — | `studentAssistant.js:276-279,419` | T2-37 |
| **R-47** | Cloud Function | `published` is selected and never filtered; the last line of defence is a sentence in the prompt | Exposure is prevented entirely by a write-side invariant in another repo | `results`/`resultsStaging` publication invariant | `studentAssistant.js:367-372,381,464`; `Result.php:1075-1117` | T2-61, T2-62 |
| **R-48** | Parent Android | A multi-tool answer discloses only the first tool; an unrecognised tool degrades to "Checked your records", which asserts nothing | `tools.firstOrNull()` | I3 | `AssistantScreen.kt:227,233` | T1-22, T1-21 |
| **R-49** | Parent Android | Three of four interactive targets are below the 48 dp minimum; the chips are the first thing a new user is invited to touch | A bare clickable `Surface` gets no `minimumInteractiveComponentSize`; M3 icon/tonal button defaults are 40 dp | WCAG 2.5.5 / Material | `AssistantScreen.kt:281-294,332-344,209-219` | T3-07…T3-10, T3-15 |
| **R-50** | Parent Android | A blind user sends a question into total silence for up to 70 s, and the answer never announces its arrival | No `liveRegion`, no `contentDescription` on the thinking bubble; no speaker prefix on message rows | — | `AssistantScreen.kt:250-263,73,113,170-201` | T3-11…T3-14, T3-15 |
| **R-51** | Parent Android | The two trust affordances — the AI disclosure and the tool chip — are the lowest-contrast text on the screen and both fail AA | `textTertiary` at 11 sp and 10.5 sp; computed 2.83:1/3.57:1 and 2.55:1/3.04:1 | Functional text meets 4.5:1 | `AssistantScreen.kt:151,245`; `ThemeVariants.kt:395,402,406,455,462,466` | T3-02, T3-04, T3-05 |
| **R-52** | Parent Android | The required AI disclosure is removed by the user's first message and never returns; its presence depends on process lifetime | The whole intro block is gated on `messages.isEmpty()` | Disclosure durability (DPDP, minors) | `AssistantScreen.kt:112,148-153` | T3-01 |
| **R-53** | Parent Android | Every size is a hardcoded literal; at 1.3× the two-line top bar clips first, then both chip types | Nothing derives from `MaterialTheme.typography`, unlike sibling screens | Layout survives 2.0× | `AssistantScreen.kt:68,75,138,143,151,192,245,292` | T3-16, T3-17 |
| **R-54** | Parent Android | `UnavailableNotice` holds unbounded server text in a non-scrolling centred `Box` and clips with no affordance | No `verticalScroll` — the one place the file's own header promise is violated | Every full-screen message must scroll | `AssistantScreen.kt:36-42,348-354` | T3-19 |
| **R-55** | Parent Android | In landscape with the IME up the thread is squeezed to a sliver, worst at first run when the intro needs the most room | Top bar + composer + `imePadding` + a 40 dp chip row against a ~360 dp viewport | The recurring Compose clipping bug class (CLAUDE.md) | `AssistantScreen.kt:97,104-115,117,130-154` | T3-18 |
| **R-56** | Parent Android | Process death silently empties an active session, including a half-typed question, and nothing tells the user | No `SavedStateHandle`, no `rememberSaveable`; the documented privacy stance covers disk and Firestore, not the saved-instance bundle | — | `AssistantViewModel.kt:32-33`; `AssistantMessage.kt:5-8` | T2-18, T1-52 |
| **R-57** | Cloud Function | The least-changing document in the feature is read on every single request, on the latency critical path | No in-instance memo, no TTL cache | — | `studentAssistant.js:148-151,158,162` | T2-68 |
| **R-58** | Cloud Function vs app | If any tenant has `schoolCode ≠ schoolId`, the CF and the Parent app read **different documents** | The CF keys off `school_id` alone; both Parent repositories resolve `schoolCode ?: schoolId` | G1-adjacent | `AttendanceFirestoreRepository.kt:117-126`; `studentAssistant.js:150,193,293` | T2-60 |
| **R-59** | Cloud Function | A legacy student doc carrying only `class: "10"` yields `"Class 10"` ≠ `"Class 10th"` — a silent empty result | CF-only legacy fallback the app does not have | — | `studentAssistant.js:180,111-113`; `Entity_firestore_sync.php:164-209` | T2-56 |
| **R-60** | Firestore | Quota and log documents orphan permanently; a mid-day school transfer silently resets the day's allowance | The quota key embeds `schoolId`; nothing cleans up either collection | I6 (Q-I2); Retention | `studentAssistant.js:193,622`; `Data_service.php:344-346` | T2-57, T2-58, T2-59 |
| **R-61** | Cloud Function | The assistant will name an absent teacher for a period that has a substitute | No substitute overlay; the app has one | `[CONTESTED]` in scope only | `TimetableFirestoreRepository.kt:145-216`; `studentAssistant.js:352-364` | T2-64 |
| **R-62** | Parent Android | The feature emits no analytics event and contains no logging of any kind — a support engineer has nothing to work from | Nothing was added; the app links `firebase-analytics` and the feature uses none of it | — | `ui/assistant/*`; `AssistantRepository.kt` | T2-77 |
| **R-63** | Cloud Function | A payload whose last 20 entries are malformed silently yields an **empty** history, discarding valid earlier turns | `slice` runs before the validity filter | — | `studentAssistant.js:487,504-511` | T2-52 |
| **R-64** | Parent Android | An error and an answer are the same shape; the only difference is an 8%-alpha tint | Same bubble, radius, border, size and position | — | `AssistantScreen.kt:175-179,194-198`; `ThemeVariants.kt:412,472` | T3-25, T3-26 |
| **R-65** | Parent Android | There is no way to start over; the capability hints are withdrawn the instant the user shows interest | Intro and chips are both gated on an empty transcript; no clear/new-chat action anywhere | — | `AssistantScreen.kt:62-91,112,117` | T2-07, T3-33 |
| **R-66** | Parent Android | Send-while-thinking is silently swallowed with no feedback, during the exact wait when people press again | `send()` returns immediately on `isThinking`; the text field stays editable | A rejected input action must be visibly rejected | `AssistantViewModel.kt:39`; `AssistantScreen.kt:121,313-330` | T2-84 |
| **R-67** | Parent Android | No way to cancel a slow turn; Back abandons a billed call | No `Job` handle, no stop control | I6 | `AssistantViewModel.kt:57`; `AssistantScreen.kt:119-124` | T2-85 |
| **R-68** | Parent Android | The two most likely failure messages a real user meets are raw untranslated English | The ViewModel prefers `e.message` over the translated resource | — | `AssistantViewModel.kt:96,104` | T1-63 |
| **R-69** | Cloud Function | A missing index becomes "That lookup failed." — correct behaviour, useless message | The CF has no widen-and-refilter fallback (which is right); only the copy is wrong | — | `studentAssistant.js:596-602`; `HomeworkFirestoreRepository.kt:94-131` | T2-43 |

---

## P3

| ID | Surface | Symptom | Root cause | Invariant at risk | Citation | UAT rows |
|---|---|---|---|---|---|---|
| **R-70** | Cloud Function | `cacheWriteTokens` is structurally always zero and will read as a broken cache during a cost investigation | Initialised, never assigned, always logged | — | `studentAssistant.js:529,635` | T1-46 |
| **R-71** | Cloud Function | The deploy comment names an `ANTHROPIC_API_KEY` the function does not use and claims it files helpdesk tickets | Stale since the 2026-08-30 rework | Deploy correctness | `functions/index.js:666-670`; `studentAssistant.js:478-480,517-521` | T0-09, T0-10 |
| **R-72** | Cloud Logging | A pseudonymous child identifier reaches Cloud Logging on error paths, where the IAM audience is broader than Firestore's | `logger.error` carries `{tool, schoolId, studentId, err}` on tool failure | — | `studentAssistant.js:597-598` | T2-75 |
| **R-73** | Parent Android | A generically named app-wide string (`common_back`) is owned by one feature's strings file, in all six locales | — | — | `values*/strings_assistant.xml:12,16` | T3-47 |
| **R-74** | Parent Android | The Support back-stack comment is inaccurate for the assistant caller | Entered via the assistant the stack differs from the one the comment describes; the behaviour is arguably better | — | `NavGraph.kt:849-857` | T1-27 |
| **R-75** | Parent Android | This is the **Parent** app, yet the copy addresses the reader as the student ("your attendance", "My attendance") | — | Copy addresses the person holding the phone | `AssistantScreen.kt:228-233`; `strings_assistant.xml:24-34` | T3-31 |
| **R-76** | Parent Android | On a tablet the chrome stretches and the content does not | No `WindowSizeClass`, no container width cap | — | `AssistantScreen.kt:187,274-279,306-345` | T3-20 |
| **R-77** | Cloud Function | An unknown tool name short-circuits with no log line at all | The `if (!impl)` guard returns silently | I5 (the guard itself is correct) | `studentAssistant.js:578,582` | T2-42 |
| **R-78** | Cloud Function | If the model ever returns text alongside a `functionCall`, that text is silently dropped on every tool turn | A response is treated as final only when `calls.length === 0` | — | `studentAssistant.js:552-554` | T2-40 |
| **R-79** | Cloud Function | The harness the code's own comment names does not exist, and no npm script runs the one that does | `test_assistant.js` vs `_smoke_assistant.js`; no `scripts` block | — | `studentAssistant.js:646-648`; `functions/package.json` | T2-76 |
| **R-80** | Auth claims | `student_id` is emitted snake-only with no camel twin; the CF's fallback is dead code | The dual-emit contract is complete for `school_id` and incomplete for `student_id` | **Z1** | `Firebase.php:855-857`; `studentAssistant.js:126` | T1-31 |

---

## Controls that HOLD — protect these against regression

Recorded so a fix for something above does not quietly repeal one of these. Each has a UAT row
whose only job is to notice if it breaks.

| Control | Why it holds | Defended by | Row |
|---|---|---|---|
| **I1 structural core** — no tool schema accepts a `studentId`, `schoolId`, collection name or session, so no prompt in any language can widen a query | Verified across all six declarations; dispatch is a closed table with an unknown-name guard | **nothing** — no test, no schema lint, no CI check | T0-17, T0-36, T2-76 |
| **I5 read-only** — no write reaches school data | `TOOL_IMPL` contains five reads and one pure function; the former helpdesk writer was removed with its reasoning | **nothing** — the `_test` export exposes `TOOL_IMPL` for exactly the assertion nobody wrote | T0-33, T2-76 |
| **Role gate** — staff and admin tokens are refused | `role ∈ {student, parent}` enforced in code, not merely documented | the gate itself | T0-16 |
| **Fail-closed on a blank session** | Explicit, with a logged rationale — stronger than the Parent app's own attendance path | the check itself | T0-43 |
| **Quota cap** — the counter never reaches 31 | The guard is inside the transaction, before `tx.set` | one writer only (an emergent property, not a structural one) | T0-20, T2-47, T2-48 |
| **Server-side clock** — quota buckets are not caller-influenceable | `new Date()` server-side; the key is derived from verified claims | the key derivation | T2-16 |
| **`assistantLogs` / `assistantQuota` are client-unreachable** | The rules catch-all denies both | the **default branch**, not a named block — it survives only until someone adds a broader ancestor match | T0-12 |
| **`MAX_TURNS` is live** — 500 turns are safely capped before the model call | `.slice(-MAX_TURNS)` applied server-side | the slice | T2-51 |
| **Every tool query has a serving index and leads with `schoolId`** | All four declared with exact matches; no cross-tenant sweep exists in this module | `firestore.indexes.json` (declared ≠ deployed) | T0-11 |
| **The decision NOT to write into `supportTickets`** | Ticket numbering, reporter identity and the push chain are owned by Support Desk | the removal comment | T1-28 |
