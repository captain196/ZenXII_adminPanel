{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "database": {
    "rules": "database.rules.json"
  },
  "functions": [
    {
      "source": "../functions",
      "codebase": "notice-circular-push",
      "ignore": ["node_modules"],
      "runtime": "nodejs20"
    }
  ]
}
